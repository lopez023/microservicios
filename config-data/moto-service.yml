server:
  port: 8003

application:
  name: Moto


eureka:
  client:
    fetch-registry: true
    registry-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
    instance:
      hostname: localhost
